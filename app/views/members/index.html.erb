<% @members.each do |member| %>
  <%= link_to "#{member.name}", "/members/#{member.id}/" %><br> <br>
  <%= member.instrument %> <br>
  <%= link_to 'Update Member', "/members/#{member.id}/edit" %> <br>
  <%= link_to 'Delete Member', "/members/#{member.id}", method: :delete %><br>
  <br>
<% end %>

