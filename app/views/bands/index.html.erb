<% @bands.order_by_most_recent.each do |band| %>
  <%= band.name  %><br>

  <p>Booked: <%= band.booked %></p>
  <p>Date added: <%= band.date_added %></p><br>
<% end %>

<%= link_to "New Band", "/bands/new" %>